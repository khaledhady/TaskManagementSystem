<h1>Listing project_user_records</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project_user_records.each do |project_user_record| %>
  <tr>
    <td><%= link_to 'Show', project_user_record %></td>
    <td><%= link_to 'Edit', edit_project_user_record_path(project_user_record) %></td>
    <td><%= link_to 'Destroy', project_user_record, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project user record', new_project_user_record_path %>
