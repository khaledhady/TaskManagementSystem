<%= form_for(@project_user_record) do |f| %>
  <% if @project_user_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_user_record.errors.count, "error") %> prohibited this project_user_record from being saved:</h2>

      <ul>
      <% @project_user_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
